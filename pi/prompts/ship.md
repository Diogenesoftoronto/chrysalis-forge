Implement the following task directly:

$@

Requirements:

- inspect the codebase before changing it
- use terminal-first workflows
- prefer minimal, verifiable changes
- run the relevant checks after each logical unit of work
